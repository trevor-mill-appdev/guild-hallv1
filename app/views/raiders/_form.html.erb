<%= form_with(model: raider) do |form| %>
  <% if raider.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(raider.errors.count, "error") %> prohibited this raider from being saved:</h2>

      <ul>
        <% raider.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :serial %>
    <%= form.number_field :serial %>
  </div>

  <div class="field">
    <%= form.label :image_url %>
    <%= form.text_field :image_url %>
  </div>

  <div class="field">
    <%= form.label :owner_id %>
    <%= form.text_field :owner_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

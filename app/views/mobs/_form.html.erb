<%= form_with(model: mob) do |form| %>
  <% if mob.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(mob.errors.count, "error") %> prohibited this mob from being saved:</h2>

      <ul>
        <% mob.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :owner_id %>
    <%= form.text_field :owner_id %>
  </div>

  <div class="field">
    <%= form.label :serial %>
    <%= form.text_field :serial %>
  </div>

  <div class="field">
    <%= form.label :image_url %>
    <%= form.text_field :image_url %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

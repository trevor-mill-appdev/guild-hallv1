<h1><%= current_user.username %>'s Landing Page</h1>

<h2>Raiders</h2>
<ul>
  <% current_user.raiders.each do |raider| %>
    <li>
      <%= raider.serial %>
      <%= image_tag raider.image %>
    </li>
  <% end %>
</ul>

<h2>Stash</h2>
<ul>
  <% current_user.stashes.each do |stash| %>
    <li>
      <%= Material.where(:id => stash.material_id ).first.name %>: <%= stash.quantity %>
    </li>
  <% end %>
</ul>

<h2>Inbox</h2>